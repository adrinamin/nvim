return {
    "nvim-lua/plenary.nvim", --lua functions that many plugins use
    --"christoomey/vim-tmux-nanvigator", --tmux & pslit window navigation, currently there is an issue with that the repo cannot be found!
    -- find an alternative
}
